/*eslint-disable*/
import React from 'react';

const HeavyComponent = () => (
  <div>
    <h2>Heavy Component Loaded Successfully!</h2>
    <p>This component is loaded dynamically using react-loadable.</p>
  </div>
);

export default HeavyComponent;
